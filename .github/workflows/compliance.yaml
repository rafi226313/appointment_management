name: Compliance Check
on:
  pull_request:
    types: [opened, synchronize, reopened]
permissions:
  pull-requests: write
  contents: read
jobs:
  compliance-check:
    runs-on: ubuntu-latest
    steps:
      # Pinned to @v1.4 (tracked-source packaging + per-file size cap).
      - uses: HIPAACKR/pr-compliance-check@v1.5
        with:
          api-key: ${{ secrets.COMPLIANCE_API_KEY }}
          frameworks: '["hipaa"]'
